pkgname=patatahooligan-devel
pkgver=2025.04.09
pkgrel=1
pkgdesc='Meta-package for common development tools.'
arch=('any')
license=('none')
depends=(
    'docker'
    'docker-buildx'
    'docker-compose'
    'gcc'
    'gdb'
    'git-crypt'
    'git-lfs'
    # Some scripts might expect hostname to be available because it's a
    # common executable across distros. debops is an example of this.
    'inetutils'
    'pyenv'
    'python-pipenv'
    'ripgrep'
)
